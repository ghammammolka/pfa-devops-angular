name: Docker CI Pipeline

on:
  push:
    branches:
      - main

jobs:

  build-and-push:
    runs-on: ubuntu-latest

    steps:

      - name: Checkout code
        uses: actions/checkout@v4

      - name: Login Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build Backend Image
        run: |
          docker build -t molkaghammam/cours-backend:latest ./cours-app

      - name: Push Backend Image
        run: |
          docker push molkaghammam/cours-backend:latest

      - name: Build Frontend Image
        run: |
          docker build -t molkaghammam/cours-frontend:latest ./front-cours

      - name: Push Frontend Image
        run: |
          docker push molkaghammam/cours-frontend:latest